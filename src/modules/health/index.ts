/**
 * @fileoverview
 * Health module entry point — exports all health-related routes.
 * Keeps module imports clean and consistent across the app.
 */

import healthRoutes from "./routes/health.routes";

export default healthRoutes;
